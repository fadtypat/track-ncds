# Test Cases — รับประกันคุณภาพเชิงปฏิบัติการของระบบ

Test case แบบ step-by-step ของฟีเจอร์
[[../../../02-design/feature-list#5. รับประกันคุณภาพเชิงปฏิบัติการของระบบ (Performance, Availability, Clinical Safety, Session Security, Accessibility, Compatibility, Interoperability)|5. รับประกันคุณภาพเชิงปฏิบัติการของระบบ]]
(NFR-09–NFR-15 — ไม่รวม NFR-16 ซึ่งเป็น Won't have) อ้างอิงเกณฑ์ยอมรับจาก
[[../acceptance-criteria#5. รับประกันคุณภาพเชิงปฏิบัติการของระบบ (Performance, Availability, Clinical Safety, Session Security, Accessibility, Compatibility, Interoperability)|acceptance-criteria]]
ฟีเจอร์นี้เป็นข้อกำหนดเชิง cross-cutting ที่ครอบคลุมฟีเจอร์ที่ 1-4 ทั้งหมด จึงใช้หน้าจอจากหลายฟีเจอร์
(`patient-list.html`, `patient-detail-risk-found.html`, `patient-detail-no-risk.html`,
`access-denied.html`) จาก
[[../../../02-design/01-prototypes/20260918-01-v1/prototype|prototype v1]] เป็นตัวอย่างอ้างอิงร่วมกัน
บาง test case (Clinical Safety Validation, Security Rules Verification, Availability) เป็นการทวนสอบ
เชิงกระบวนการ/เอกสาร/automated test เบื้องหลัง ไม่ใช่การคลิกผ่านหน้าจอ prototype โดยตรง

บทบาทผู้ทดสอบ: แพทย์/พยาบาลผู้ดูแลผู้ป่วย NCD (บทบาทเดียวในขอบเขตของระบบ) — บาง test case จำลองบทบาท
ทีมพัฒนา/ผู้ดูแลระบบ (deployment/QA process) แทน เนื่องจากเป็นการทวนสอบกระบวนการ ไม่ใช่พฤติกรรมที่
ผู้ใช้ปลายทางโต้ตอบโดยตรง (ระบุไว้ในคอลัมน์ Precondition ของแต่ละแถว)

**อัปเดต 2026-09-22:** `[[../../../02-design/02-technical/architecture]]`/`[[../../../02-design/02-technical/api-spec]]`/
`[[../../../02-design/02-technical/db-spec]]`/`detailed-design/*` ถูกอัปเดตให้อ้างอิงกลไกจริงของ
NFR-09 (Firestore composite index เท่านั้น ไม่มี caching), NFR-12 (client custom inactivity timer —
**ไม่มี server-side token revocation**), NFR-13 (WCAG 2.1 AA + Heroicons + Lighthouse) และ NFR-15
(browserslist `">0.5%, last 2 versions, Firefox ESR, not dead"`) แล้ว จึงเพิ่ม TC-05-14 ถึง TC-05-18
ด้านล่างเพื่อทดสอบกลไกจริงเหล่านี้โดยตรง (แทนการทดสอบแบบ generic เดิม) — TC-05-01 ถึง TC-05-13 เดิม
ยังคงใช้ได้และไม่มีการเปลี่ยนหมายเลข เพื่อไม่กระทบการอ้างอิงจาก [[../test-plan]]

| รหัส | ชื่อ test case | Precondition | ขั้นตอน | Test Data | ผลลัพธ์ที่คาดหวัง | AC ที่ทดสอบ | FR/NFR | ระดับความสำคัญ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-05-01 | หน้าจอหลักตอบสนองภายใน 2 วินาที (Happy path) | ผู้ใช้ผ่านการตรวจสอบสิทธิ์แล้ว และมีผู้ป่วยอย่างน้อย 1 รายในความดูแล ระบบทำงานในสภาพแวดล้อมทดสอบปกติ | 1. เปิดหน้าจอ `patient-list.html` แล้วจับเวลาตั้งแต่สั่งการ (ค้นหา/เปิดหน้าจอ) จนข้อมูลแสดงครบ<br>2. เปิดหน้าจอ `patient-detail-risk-found.html` (ประวัติวินิจฉัย/ผลตรวจ lab/ผลวิเคราะห์ความเสี่ยง) แล้วจับเวลาเช่นเดียวกัน | HN = 6500123 (ตัวอย่างผู้ป่วยในความดูแล) | ทุกหน้าจอ (ค้นหา/รายชื่อผู้ป่วย, ประวัติวินิจฉัย, ผลตรวจ lab, ผลวิเคราะห์ความเสี่ยง) แสดงข้อมูลครบถ้วนภายในเวลาน้อยกว่า 2 วินาที นับจากผู้ใช้งานสั่งการ | [[../acceptance-criteria#NFR-09 (สูง) — Performance\|NFR-09 AC-1]] | NFR-09 | สูง |
| TC-05-02 | ขอบเขตการทดสอบ performance จำกัดเฉพาะ single-request (design review) | ทีมทดสอบเตรียมออกแบบแผนทดสอบ performance ของระบบ | 1. ตรวจสอบแผนทดสอบ performance ที่ทีมทดสอบออกแบบ<br>2. ยืนยันว่าแผนครอบคลุมเฉพาะเวลาตอบสนองของคำขอเดี่ยว ไม่รวม load/stress/concurrency test | - | แผนทดสอบระบุชัดเจนว่าไม่ครอบคลุม load/stress/concurrency test เนื่องจากรายละเอียดเชิงเทคนิคยังไม่ถูกกำหนดใน spec ต้นทาง (นอกขอบเขตของรอบทดสอบนี้) | [[../acceptance-criteria#NFR-09 (สูง) — Performance\|NFR-09 AC-2]] | NFR-09 | สูง |
| TC-05-03 | ยืนยันไม่ตั้ง SLA เกินมาตรฐาน Firebase/Google Cloud (design/configuration review) | `technology-stack.md` ระบุ Firebase/Google Cloud เป็น hosting platform หลักแล้ว | 1. ตรวจสอบเอกสาร `technology-stack.md` และการตั้งค่า deployment configuration<br>2. ยืนยันว่าไม่มีการตั้งค่า SLA เพิ่มเติมที่เข้มงวดกว่ามาตรฐานของ Firebase/Google Cloud | - | ไม่พบการตั้งค่า SLA ที่เข้มงวดกว่ามาตรฐานของผู้ให้บริการ ระบบอ้างอิง SLA มาตรฐานของ Firebase/Google Cloud เป็นเป้าหมาย uptime เท่านั้น | [[../acceptance-criteria#NFR-10 (กลาง) — Availability\|NFR-10 AC-1]] | NFR-10 | กลาง |
| TC-05-04 | มีหลักฐานยืนยันจากแพทย์ผู้เชี่ยวชาญก่อน deploy rule ใหม่ (Happy path, process review) | มีการเพิ่ม/แก้ไข rule การจับคู่โรคหลัก→โรคแทรกซ้อน หรือค่า threshold lab ใน Risk Rule Engine ที่รอ deploy | 1. ตรวจสอบหลักฐาน/เอกสารการยืนยันจากแพทย์ผู้เชี่ยวชาญสำหรับ rule/threshold ที่จะ deploy<br>2. ยืนยันว่าหลักฐานครบถ้วนก่อนดำเนินการ deploy | - | พบหลักฐานการยืนยันจากแพทย์ผู้เชี่ยวชาญครบถ้วนก่อน deploy ทุกครั้ง | [[../acceptance-criteria#NFR-11 (สูง) — Clinical Safety Validation\|NFR-11 AC-1]] | NFR-11 | สูง |
| TC-05-05 | ไม่ deploy rule/threshold ที่ยังไม่ผ่านการยืนยัน (edge case) | มี rule/threshold ใหม่ที่ยังไม่ผ่านการยืนยันจากแพทย์ผู้เชี่ยวชาญ | 1. ตรวจสอบ pipeline/กระบวนการ deploy ว่าปฏิเสธ/ระงับการ deploy เมื่อไม่มีหลักฐานการยืนยันแนบมา | - | ไม่มีการ deploy โค้ด Risk Rule Engine ที่มี rule ยังไม่ผ่านการยืนยัน | [[../acceptance-criteria#NFR-11 (สูง) — Clinical Safety Validation\|NFR-11 AC-2]] | NFR-11 | สูง |
| TC-05-06 | Auto-logout เมื่อ idle เกิน 30 นาที (Happy path) | ผู้ใช้ล็อกอินเข้าระบบด้วยบทบาทแพทย์/พยาบาลผู้ดูแลผู้ป่วย NCD สำเร็จแล้ว | 1. ล็อกอินเข้าระบบ<br>2. ปล่อยให้ไม่มีการโต้ตอบกับระบบ (ไม่คลิก/พิมพ์ใดๆ) นานเกิน 30 นาที<br>3. สังเกตสถานะของระบบหลังครบเวลา | ระยะเวลา idle = 31 นาที (เกิน threshold 30 นาที) | ระบบ auto-logout ผู้ใช้งานโดยอัตโนมัติ และนำกลับไปยังหน้าจอเข้าสู่ระบบ | [[../acceptance-criteria#NFR-12 (สูง) — Session Timeout\|NFR-12 AC-1]] | NFR-12 | สูง |
| TC-05-07 | ใช้งานต่อเนื่องไม่ถูก auto-logout | ผู้ใช้ล็อกอินเข้าระบบสำเร็จแล้ว | 1. ล็อกอินเข้าระบบ<br>2. โต้ตอบกับระบบ (คลิก/เปิดหน้าจอ) เป็นระยะทุกน้อยกว่า 30 นาที ต่อเนื่องอย่างน้อย 35 นาที | ช่วงเวลาโต้ตอบทุก 10 นาที ต่อเนื่อง 35 นาที (ไม่มีช่วง idle เกิน 30 นาที) | ระบบไม่ auto-logout ผู้ใช้ยังคงใช้งานได้ตามปกติตลอดช่วงเวลาที่ทดสอบ | [[../acceptance-criteria#NFR-12 (สูง) — Session Timeout\|NFR-12 AC-2]] | NFR-12 | สูง |
| TC-05-08 | คำขอหลัง auto-logout ถูกปฏิเสธ (edge case) | ผู้ใช้ถูก auto-logout ไปแล้วเนื่องจาก idle เกิน 30 นาที (สืบเนื่องจาก TC-05-06) แต่ยังมี tab/หน้าจอเดิมค้างอยู่ | 1. หลังถูก auto-logout กลับไปที่ tab เดิมที่ยังค้างอยู่<br>2. พยายามเรียกดูข้อมูลผู้ป่วย (เช่น กดรีเฟรช หรือกดปุ่มในหน้าจอเดิม) โดยไม่ล็อกอินใหม่ | - | ระบบปฏิเสธคำขอนั้นที่ขั้นตอนตรวจสอบสิทธิ์ระดับบทบาท เช่นเดียวกับกรณีไม่มีข้อมูลยืนยันตัวตน และนำผู้ใช้กลับไปหน้าจอเข้าสู่ระบบ (แสดง `access-denied.html` หรือหน้าจอเข้าสู่ระบบ) | [[../acceptance-criteria#NFR-12 (สูง) — Session Timeout\|NFR-12 AC-3]] | NFR-12 | สูง |
| TC-05-09 | Flag ความเสี่ยงมีข้อความกำกับคู่กับสี (Happy path) | เลือกผู้ป่วยที่ผลวิเคราะห์ความเสี่ยงคือ "พบความเสี่ยง" | 1. เปิดหน้าจอ `patient-detail-risk-found.html`<br>2. ตรวจสอบ flag/สัญญาณเตือนความเสี่ยงที่แสดง | HN = 6500123 (ผลวิเคราะห์ = พบความเสี่ยง) | flag/สัญญาณเตือนความเสี่ยงมีข้อความระบุระดับความเสี่ยงกำกับคู่กับสี/ไอคอนเสมอ ไม่ใช้สีเพียงอย่างเดียว | [[../acceptance-criteria#NFR-13 (กลาง) — Accessibility\|NFR-13 AC-1]] | NFR-13 | กลาง |
| TC-05-10 | ทุกสถานะผลลัพธ์ 3 แบบมีข้อความกำกับ | มีผู้ป่วย 3 รายที่ผลวิเคราะห์ความเสี่ยงแต่ละแบบต่างกัน (พบความเสี่ยง/ไม่พบความเสี่ยงเพิ่มเติม/ข้อมูลไม่เพียงพอ) | 1. เปิดหน้าจอผลวิเคราะห์ความเสี่ยงของผู้ป่วยแต่ละรายตามลำดับทั้ง 3 แบบ<br>2. ตรวจสอบข้อความกำกับของแต่ละแบบ | ผู้ป่วย 3 ราย: HN 6500123 = พบความเสี่ยง, HN 6500456 = ไม่พบความเสี่ยงเพิ่มเติม, HN 6500789 = ข้อมูลไม่เพียงพอ | ทุกแบบมีข้อความกำกับที่อ่านเข้าใจได้ชัดเจนโดยไม่ต้องพึ่งพาสีเพียงอย่างเดียว | [[../acceptance-criteria#NFR-13 (กลาง) — Accessibility\|NFR-13 AC-2]] | NFR-13 | กลาง |
| TC-05-11 | Automated test ผ่าน Firebase Emulator Suite ครบ 4 กรณี (Happy path — อัปเดต 2026-09-24: กรณี (ง) แก้ไขจาก "custom claims" เป็น `email_verified`) | Firestore Security Rules สำหรับ `patientAssignments`/`users` ถูกเขียน/แก้ไขแล้ว รอ deploy | 1. รัน automated test ผ่าน Firebase Emulator Suite (`@firebase/rules-unit-testing`)<br>2. ตรวจสอบผลทดสอบครบ 4 กรณี (ก)-(ง) | 4 กรณีทดสอบ: (ก) ผู้ใช้ไม่มี PatientAssignment เลย (ข) ผู้ใช้มี PatientAssignment เฉพาะบางราย (ค) บัญชีถูกระงับ (isActive=false) (ง) บัญชียังไม่ยืนยันอีเมล (email_verified=false) แม้ role/isActive/PatientAssignment ถูกต้องครบ | automated test ผ่านครบทั้ง 4 กรณี ก่อนอนุญาตให้ deploy Security Rules สู่ระบบจริง | [[../acceptance-criteria#NFR-14 (สูง) — Security Rules Verification\|NFR-14 AC-1]] | NFR-14 | สูง |
| TC-05-12 | ไม่ deploy เมื่อ automated test ไม่ผ่าน (edge case) | automated test ของ Security Rules กรณีใดกรณีหนึ่งใน 4 กรณีไม่ผ่าน | 1. ตรวจสอบ pipeline/กระบวนการ deploy เมื่อพบว่า automated test ไม่ผ่าน | - | ระบบ/กระบวนการ deploy ต้องไม่อนุญาตให้ deploy Security Rules ชุดนั้นจนกว่า automated test จะผ่านครบทุกกรณี | [[../acceptance-criteria#NFR-14 (สูง) — Security Rules Verification\|NFR-14 AC-2]] | NFR-14 | สูง |
| TC-05-13 | แสดงผลถูกต้องบน Chrome/Edge/Firefox บน desktop/tablet | ระบบ deploy พร้อมใช้งานในสภาพแวดล้อมทดสอบ | 1. เปิดหน้าจอค้นหา/รายชื่อผู้ป่วย, ประวัติวินิจฉัย, ผลตรวจ lab, ผลวิเคราะห์ความเสี่ยง บน Chrome เวอร์ชันล่าสุด<br>2. ทำซ้ำบน Edge เวอร์ชันล่าสุด<br>3. ทำซ้ำบน Firefox เวอร์ชันล่าสุด<br>4. ทำซ้ำแต่ละ browser บนอุปกรณ์ desktop และ tablet | Browser = Chrome/Edge/Firefox (เวอร์ชันล่าสุด ณ ขณะทดสอบ), อุปกรณ์ = desktop, tablet | ทุกหน้าจอแสดงผลและทำงานได้ถูกต้องครบถ้วนบน browser/อุปกรณ์ทั้งหมดที่ระบุ | [[../acceptance-criteria#NFR-15 (กลาง) — Browser/Device Compatibility\|NFR-15 AC-1]] | NFR-15 | กลาง |
| TC-05-14 | Composite index ครบตาม db-spec — ไม่มี unindexed query warning (Happy path, config review) | `firestore.indexes.json` deploy แล้วในสภาพแวดล้อมทดสอบ (Firebase Emulator Suite หรือ dev project) ตามรายการ index ใน [[../../../02-design/02-technical/db-spec\|db-spec]] | 1. รัน query จริงของแต่ละ operation: Operation 0 (`patientAssignments` filter `userId`+`patientHn` หรือ `patientFullName`), Operation 1 (`ncdDiagnoses` filter `patientId` sort `diagnosedAt`), Operation 2/3 (`labResults` filter `patientId`+`testType` sort `testedAt`), Operation 5 (`auditLogRecords` ทั้ง 4 รูปแบบ filter)<br>2. ตรวจสอบ log/console ว่าไม่มี error "The query requires an index" | HN = 6500123, ช่วงเวลาค้นหา audit trail ตัวอย่าง = 2026-09-01 ถึง 2026-09-22 | ทุก query รันสำเร็จโดยไม่มี Firestore unindexed query error/warning — composite index ที่ deploy ตรงกับ query pattern จริงครบทุกจุด | [[../acceptance-criteria#NFR-09 (สูง) — Performance\|NFR-09 AC-3]] | NFR-09 | สูง |
| TC-05-15 | บันทึก baseline เวลาตอบสนองเมื่ออ่านข้อมูลอ้างอิงคงที่ซ้ำทุก request (ไม่มี caching — known limitation) | ระบบทำงานในสภาพแวดล้อมทดสอบที่สเกลใกล้เคียง ~20 concurrent users ตามเป้าหมาย MVP | 1. เรียก Operation 3 (ผลวิเคราะห์ความเสี่ยง) ซ้ำหลายครั้งติดต่อกันสำหรับผู้ป่วยคนละราย เพื่อให้ Cloud Functions อ่าน `ComplicationRiskThreshold` ซ้ำทุกครั้ง (ไม่มี in-memory cache)<br>2. บันทึกเวลาตอบสนองแต่ละครั้งเป็น baseline | เรียก Operation 3 ต่อเนื่อง 10 ครั้ง ด้วย HN คนละราย (เช่น 6500123, 6500456, ...) | เวลาตอบสนองทุกครั้งยังคงน้อยกว่า 2 วินาทีตาม AC-1 ที่สเกลปัจจุบัน — ผลลัพธ์นี้ถือเป็น known limitation (ไม่มี caching) ที่ยอมรับได้ตามการออกแบบ ไม่ใช่ defect ต้องบันทึกผลไว้เป็น baseline สำหรับทวนสอบซ้ำเมื่อข้อมูลจริงมีปริมาณมากขึ้น | [[../acceptance-criteria#NFR-09 (สูง) — Performance\|NFR-09 AC-4]] | NFR-09 | สูง |
| TC-05-16 | Security awareness — token ที่ถูกขโมย/intercept ไว้ก่อน idle ยังใช้เรียก Backend ได้จนหมดอายุตามธรรมชาติ (known risk, ไม่ใช่ defect) | มี Firebase ID token ที่ capture ไว้ก่อนหน้า (จำลองสถานการณ์ token ถูกดักจับ/อุปกรณ์สูญหาย) ของผู้ใช้ที่ idle เกิน 30 นาทีไปแล้วแต่ client เดิมไม่ได้เรียก `signOut()` (จำลอง client ถูกดัดแปลง/ปิด tab กะทันหันก่อน timer ทำงาน) | 1. เรียก Cloud Functions (เช่น Operation 1 — ดึงประวัติวินิจฉัย) โดยตรงด้วย token ที่ capture ไว้ (ไม่ผ่าน Client UI ปกติ) หลังเวลาผ่านไปเกิน 30 นาทีนับจาก idle แต่ยังไม่ถึงอายุ token ตามธรรมชาติ (~1 ชั่วโมง)<br>2. ตรวจสอบว่า Backend Service อนุญาต/ปฏิเสธคำขอ | Token อายุ ~45 นาทีนับจาก issue (เกิน 30 นาทีที่ NFR-12 กำหนด แต่ยังไม่ถึง ~1 ชั่วโมงที่ token หมดอายุตามธรรมชาติ) | Backend Service ยังคงอนุญาตคำขอ (ตรวจสอบเพียงว่า token ถูกต้อง/ยังไม่หมดอายุเท่านั้น) — **นี่คือผลลัพธ์ที่ถูกต้องตามการออกแบบปัจจุบัน ห้ามรายงานเป็น defect** ต้องบันทึกผลไว้เป็นหลักฐานความเสี่ยงด้านความปลอดภัยที่ยังไม่ถูกปิดใน MVP พร้อมแนบข้อเสนอแนะ mitigation (ลด token TTL / เพิ่ม server-side token revocation) ในรายงานผลทดสอบ | [[../acceptance-criteria#NFR-12 (สูง) — Session Timeout\|NFR-12 AC-4]] | NFR-12 | สูง |
| TC-05-17 | Lighthouse Accessibility Audit ผ่านเกณฑ์ก่อน deploy (WCAG 2.1 AA + Heroicons) | Design token สี/contrast ratio ใน [[../../../02-design/DESIGN\|DESIGN]] และไอคอน Heroicons ถูกใช้งานครบตามที่กำหนด | 1. เปิดหน้าจอค้นหา/รายชื่อผู้ป่วย, ประวัติวินิจฉัย, ผลตรวจ lab, ผลวิเคราะห์ความเสี่ยง<br>2. รัน Lighthouse Accessibility Audit (Chrome DevTools) บนแต่ละหน้าจอ<br>3. ตรวจสอบผล audit หมวด Contrast และหมวดที่เกี่ยวกับการสื่อความหมายด้วยสี | HN = 6500123 (ผลวิเคราะห์ = พบความเสี่ยง, ใช้แสดง flag สี+ไอคอน) | ไม่มี failed audit ในหมวด "Contrast" หรือหมวดที่เกี่ยวกับการสื่อความหมายด้วยสีเพียงอย่างเดียว และคะแนน Accessibility ผ่านเกณฑ์ที่ทีมกำหนด (เช่น ≥ 90) ทุกหน้าจอ ก่อนอนุญาตให้ deploy | [[../acceptance-criteria#NFR-13 (กลาง) — Accessibility\|NFR-13 AC-3]] | NFR-13 | กลาง |
| TC-05-18 | Browserslist config ตรงตาม technology-stack + tablet ทดสอบผ่าน Chrome DevTools emulation | React SPA build config (`package.json`/`.browserslistrc`) พร้อมตรวจสอบก่อน build/deploy | 1. เปิดไฟล์ build config ตรวจสอบค่า browserslist ที่ตั้งไว้จริง<br>2. เทียบกับค่าที่ [[../../../02-design/02-technical/technology-stack\|technology-stack]] กำหนด<br>3. จัดทำ/ตรวจสอบ browser test matrix ว่าครอบคลุม Chrome/Edge/Firefox ล่าสุด + Firefox ESR และทดสอบ tablet ผ่าน Chrome DevTools device emulation | browserslist ที่คาดหวัง = `">0.5%, last 2 versions, Firefox ESR, not dead"` | ค่า browserslist ในไฟล์ build config ตรงกับ `">0.5%, last 2 versions, Firefox ESR, not dead"` เป๊ะ และ browser test matrix ครอบคลุม Chrome/Edge/Firefox ล่าสุด + Firefox ESR บน desktop จริง และ tablet ที่จำลองผ่าน Chrome DevTools device emulation เท่านั้น (ไม่ใช้บริการ cross-browser testing เสียเงิน) | [[../acceptance-criteria#NFR-15 (กลาง) — Browser/Device Compatibility\|NFR-15 AC-2]] | NFR-15 | กลาง |

## หมายเหตุ: NFR-16 (Interoperability — Won't have)

NFR-16 ถูกกำหนดสถานะ Won't have ยืนยันโดยผู้ใช้แล้ว (ดู [[../acceptance-criteria#NFR-16 (ต่ำ — Won't have, out of scope เฟสนี้ทั้งหมด) — Interoperability (future)|acceptance-criteria — NFR-16]])
จึงไม่มี test case สำหรับ NFR-16 ในไฟล์นี้

## เอกสารที่เกี่ยวข้อง

- [[../acceptance-criteria]]
- [[../test-plan]]
- [[../../../02-design/feature-list]]
- [[../../../02-design/user-journey]]
- [[patient-search-selection]]
- [[patient-ncd-diagnosis-lab-history]]
- [[complication-risk-analysis-alert]]
- [[pdpa-data-protection-compliance]]
- [[../../../02-design/02-technical/detailed-design/patient-search-selection#Cross-cutting: คุณภาพเชิงปฏิบัติการของระบบ (NFR-09–NFR-16)|detailed-design — Cross-cutting คุณภาพเชิงปฏิบัติการของระบบ]]
- [[../../../02-design/02-technical/technology-stack|technology-stack]]
- [[../../../02-design/02-technical/architecture|architecture]]
- [[../../../02-design/02-technical/db-spec|db-spec]]
- [[../../../02-design/DESIGN|DESIGN]]
- [[../../../02-design/01-prototypes/20260918-01-v1/prototype|prototype v1]]
