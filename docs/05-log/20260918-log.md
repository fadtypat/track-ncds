# Log — 2026-09-18

## สร้าง Prototype v1 (เวอร์ชันแรกทั้งหมด) — เรียกผ่าน skill `build-prototype`

**บริบท**

`docs/02-design/01-prototypes/` ไม่มีโฟลเดอร์เวอร์ชันใดอยู่มาก่อน (มีเพียง `designsystem.html`
ซึ่งเป็นไฟล์ bundle/artifact อ้างอิงที่ใช้ดึง token มาเขียน [[DESIGN]] เท่านั้น ไม่ใช่โครงสร้างเวอร์ชัน
ตามรูปแบบโปรเจกต์ — ไม่ได้แก้ไข/ลบไฟล์นี้) จึงเป็นการสร้าง Prototype เวอร์ชันแรก (v1) ของโปรเจกต์
ไม่ใช่การแก้ไขของเดิม

**สถานะก่อนเริ่มงาน (gate check)**

- อ่าน [[backlog]], [[feature-list]], [[user-journey]], [[DESIGN]] และ spec ต้นทาง
  [[20260917-01-patient-ncd-history-lab-complication-risk]] ครบทั้งไฟล์
- ตรวจสอบรหัส FR-01–FR-05, NFR-01, NFR-02 ตรงกันครบทั้ง backlog ↔ feature-list ↔ user-journey
  ไม่มีรหัสตกหล่น → ไม่ต้องรัน `sync-feature-journey`
- ตรวจสอบ [[DESIGN]] มีเนื้อหาครบตามที่ `CLAUDE.md` กำหนด (color tokens §2, typography §3,
  spacing/radius/shadow/motion §4, component inventory §5, accessibility §6, responsive/mobile §7)
  → พร้อมใช้เป็น single source of truth ไม่ต้องหยุดงาน
- Glob `docs/02-design/01-prototypes/*` พบเฉพาะ `designsystem.html` (ไม่ใช่โฟลเดอร์เวอร์ชัน) —
  ไม่มีเวอร์ชันก่อนหน้าให้ยึด convention จึงออกแบบโครงสร้างไฟล์ใหม่โดยยึด [[DESIGN]] เป็นหลัก

**แผนที่เสนอและคำตอบยืนยันจากผู้ใช้**

เสนอแผนสร้างโฟลเดอร์ `docs/02-design/01-prototypes/20260918-01-v1/` ครอบคลุมทุก journey ใน
[[user-journey]] (default scope เพราะผู้ใช้ไม่ได้จำกัด scope) ด้วยไฟล์: `index.html`,
`patient-list.html`, `patient-detail-risk-found.html`, `patient-detail-no-risk.html`,
`access-denied.html`, `style.css`, `prototype.md` — ผู้ใช้ยืนยันตามแผนนี้ทั้งหมด ไม่มีการปรับ scope

**เอกสารที่สร้าง (ทั้งหมดใหม่ — ไม่มีของเดิมให้แก้)**

โฟลเดอร์: `docs/02-design/01-prototypes/20260918-01-v1/`

| ไฟล์ | Journey/ฟีเจอร์ | รหัส FR/NFR |
| --- | --- | --- |
| `style.css` | แปลง token จาก [[DESIGN]] §2–§7 เป็น CSS custom properties + component class ครบทุกหมวด (color, typography, spacing/radius/shadow/motion, component inventory, accessibility focus ring, responsive breakpoint 640px/1024px) | — |
| `index.html` | หน้ารวมลิงก์ทุกหน้าจอ จัดกลุ่มตาม journey พร้อม badge บทบาทผู้ใช้ และตาราง journey/FR ↔ ไฟล์ | — |
| `patient-list.html` | ฟีเจอร์ 3 — ค้นหา/เลือกผู้ป่วยในความดูแล | FR-05, NFR-02 |
| `patient-detail-risk-found.html` | ฟีเจอร์ 1 + ฟีเจอร์ 2 (แขนง "พบ" ความเสี่ยง) — ตัวอย่างผู้ป่วยสมชาย ใจกล้า | FR-01, FR-02, FR-03, FR-04, NFR-01 |
| `patient-detail-no-risk.html` | ฟีเจอร์ 1 + ฟีเจอร์ 2 (แขนง "ไม่พบ" ความเสี่ยง) — ตัวอย่างผู้ป่วยวิชัย ถุงลมดี | FR-01, FR-02, FR-03, FR-04, NFR-01 |
| `access-denied.html` | สถานะ guard NFR-02 (บทบาทไม่ถูกต้อง / ผู้ป่วยไม่อยู่ในความดูแล) | NFR-02 |
| `prototype.md` | สรุปขอบเขต, ตาราง journey/FR ↔ ไฟล์, หมายเหตุ simplification ของ mockup data, จุดที่เน้นตาม NFR/หลักการออกแบบ | — |

**การตีความ/derived token ที่ไม่ได้ระบุตรงๆ ใน DESIGN.md**

`slate-300`, `slate-400`, `sky-300` ไม่อยู่ในตาราง §2.1 ของ [[DESIGN]] แต่ถูกอ้างถึงโดยชื่อ semantic
token ในเอกสารเดียวกัน (`--border-strong`, `--text-faint`, `--trend-flat`, `--border-hover`) จึงเติม
เป็นค่ามาตรฐาน Tailwind ของเฉดเดียวกัน (ไม่ใช่การกำหนดสีใหม่นอกจานสี) — ระบุเป็น comment ใน
`style.css` และหมายเหตุใน `prototype.md` ด้วย ไม่ได้ใช้ AskUserQuestion เพิ่มเติมเพราะเป็นการเติม
เฉดที่ขาดหายในสเกลเดียวกันที่มีอยู่แล้ว ไม่ใช่การเดาโทนสี/สไตล์ใหม่

**คำถามอื่นที่ถามระหว่างทางและคำตอบ**

ไม่มีคำถามเพิ่มเติมนอกจากการยืนยันแผน scope ในข้อบน — รายละเอียดฟอร์ม/field ทั้งหมดดึงมาจาก
[[feature-list]], [[user-journey]], [[DESIGN]] §5 (component inventory) และ spec ต้นทางได้ครบถ้วน
โดยไม่ต้องถามเพิ่ม

**Self-check ก่อนจบงาน**

อ่านไฟล์ HTML ทั้ง 5 ไฟล์ + `style.css` กลับมาตรวจแล้ว — `href` ของ proto-bar และปุ่ม action ทุกไฟล์
ตรงกับชื่อไฟล์จริงที่สร้าง ไม่มีลิงก์ตาย, ทุกไฟล์อ้างอิง `style.css` ถูกต้อง, ไม่พบ tag ที่เปิดไม่ปิดหรือ
syntax error ที่เห็นได้จากการอ่าน **ยังไม่ได้ตรวจด้วยเบราว์เซอร์จริง** (agent ไม่มีเครื่องมือเปิดเบราว์เซอร์
ในงานนี้) — แนะนำให้เปิดไฟล์จริงตรวจสอบ responsive behavior ที่ breakpoint มือถือ/แท็บเล็ตในเทรดหลัก
เป็น follow-up

## เอกสารที่เกี่ยวข้อง

- [[feature-list]]
- [[user-journey]]
- [[DESIGN]]
- [[backlog]]
- [[20260917-01-patient-ncd-history-lab-complication-risk]]
